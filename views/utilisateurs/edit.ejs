<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modifier Utilisateur</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
  <%- include('../partials/header') %>

  <main class="max-w-2xl mx-auto px-4 py-8">

    <!-- En-tête -->
    <div class="mb-8 flex items-center">
      <a href="/front/utilisateurs" class="text-blue-600 hover:text-blue-800 mr-4">
        <i class="fas fa-arrow-left"></i>
      </a>
      <div>
        <h1 class="text-2xl font-bold text-gray-800">Modifier un utilisateur</h1>
        <p class="text-gray-600 mt-1">ID : #<%= utilisateur.idUtilisateur %></p>
      </div>
    </div>

    <!-- Carte -->
    <div class="bg-white rounded-xl shadow-lg border border-gray-200 overflow-hidden">

      <!-- Header -->
      <div class="bg-gradient-to-r from-blue-600 to-blue-700 text-white p-6">
        <div class="flex items-center">
          <div class="w-12 h-12 bg-white/20 rounded-lg flex items-center justify-center mr-4">
            <i class="fas fa-user-edit text-xl"></i>
          </div>
          <div>
            <h2 class="text-xl font-bold">Modification</h2>
            <p class="text-blue-100">Mettez à jour les informations de l'utilisateur</p>
          </div>
        </div>
      </div>

      <!-- Formulaire -->
      <form method="POST" class="p-8">

        <!-- MOT DE PASSE ACTUEL CACHÉ -->
        <input type="hidden" name="motDePasse" value="<%= utilisateur.motDePasse %>">

        <!-- Erreur -->
        <% if (error) { %>
          <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
            <p class="text-red-700"><%= error %></p>
          </div>
        <% } %>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">

          <!-- Nom -->
          <div>
            <label class="block text-sm font-medium mb-2">Nom</label>
            <input type="text" name="nom" value="<%= utilisateur.nom %>" required
              class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500">
          </div>

          <!-- Prénom -->
          <div>
            <label class="block text-sm font-medium mb-2">Prénom</label>
            <input type="text" name="prenom" value="<%= utilisateur.prenom %>" required
              class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500">
          </div>

        </div>

        <!-- EMAIL CORRIGÉ -->
        <div class="mb-6">
          <label class="block text-sm font-medium mb-2">Adresse email</label>
          <input type="email" name="adresseEmail" value="<%= utilisateur.adresseEmail %>" required
            class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500">
        </div>

        <!-- Téléphone -->
        <div class="mb-6">
          <label class="block text-sm font-medium mb-2">Téléphone</label>
          <input type="text" name="telephone" value="<%= utilisateur.telephone %>" required
            class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500">
        </div>

        <!-- Nouveau mot de passe -->
        <div class="mb-8">
          <label class="block text-sm font-medium mb-2">
            Nouveau mot de passe (optionnel)
          </label>
          <input type="password" name="newPassword"
            placeholder="Laisser vide pour ne pas changer"
            class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500">
        </div>

        <!-- Boutons -->
        <div class="flex justify-between pt-6 border-t">
          <a href="/front/utilisateurs" class="text-gray-600 hover:text-gray-800">Retour</a>

          <button type="submit"
            class="inline-flex items-center px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
            <i class="fas fa-save mr-2"></i>Sauvegarder
          </button>
        </div>

      </form>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
