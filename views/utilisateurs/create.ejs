<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ajouter Utilisateur - Détection de Fraude</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
  <%- include('../partials/header') %>

  <main class="max-w-2xl mx-auto px-4 py-8">
    <!-- En-tête -->
    <div class="mb-8">
      <div class="flex items-center">
        <a href="/front/utilisateurs" class="text-blue-600 hover:text-blue-800 mr-4">
          <i class="fas fa-arrow-left"></i>
        </a>
        <div>
          <h1 class="text-2xl font-bold text-gray-800">Ajouter un utilisateur</h1>
          <p class="text-gray-600 mt-1">Créez un nouveau compte utilisateur</p>
        </div>
      </div>
    </div>

    <!-- Carte du formulaire -->
    <div class="bg-white rounded-xl shadow-lg border border-gray-200 overflow-hidden">
      <!-- En-tête colorée -->
      <div class="bg-gradient-to-r from-green-600 to-green-700 text-white p-6">
        <div class="flex items-center">
          <div class="w-12 h-12 bg-white/20 rounded-lg flex items-center justify-center mr-4">
            <i class="fas fa-user-plus text-xl"></i>
          </div>
          <div>
            <h2 class="text-xl font-bold">Nouvel utilisateur</h2>
            <p class="text-green-100">Remplissez les informations du compte</p>
          </div>
        </div>
      </div>

      <!-- Formulaire -->
      <form method="POST" action="/front/utilisateurs/create" class="p-8">
        <!-- Message d'erreur -->
        <% if (error) { %>
          <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
            <div class="flex items-center">
              <i class="fas fa-exclamation-circle text-red-500 mr-3"></i>
              <p class="text-red-700"><%= error %></p>
            </div>
          </div>
        <% } %>

        <!-- Grille 2 colonnes -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
          <!-- Nom -->
          <div>
            <label for="nom" class="block text-sm font-medium text-gray-700 mb-2">
              <i class="fas fa-user mr-2 text-gray-500"></i>Nom
            </label>
            <input 
              type="text" 
              id="nom" 
              name="nom" 
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
              placeholder="Nom de l'utilisateur"
            >
          </div>

          <!-- Prénom -->
          <div>
            <label for="prenom" class="block text-sm font-medium text-gray-700 mb-2">
              <i class="fas fa-user mr-2 text-gray-500"></i>Prénom
            </label>
            <input 
              type="text" 
              id="prenom" 
              name="prenom" 
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
              placeholder="Prénom de l'utilisateur"
            >
          </div>
        </div>

        <!-- Email -->
        <div class="mb-6">
          <label for="adresseEmail" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-envelope mr-2 text-gray-500"></i>Adresse email
          </label>
          <input 
            type="email" 
            id="adresseEmail" 
            name="adresseEmail" 
            required
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
            placeholder="exemple@email.com"
          >
        </div>

        <!-- Téléphone -->
        <div class="mb-6">
          <label for="telephone" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-phone mr-2 text-gray-500"></i>Téléphone
          </label>
          <input 
            type="text" 
            id="telephone" 
            name="telephone" 
            required
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
            placeholder="+33 1 23 45 67 89"
          >
        </div>

        <!-- Mot de passe -->
        <div class="mb-8">
          <label for="motDePasse" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-lock mr-2 text-gray-500"></i>Mot de passe
          </label>
          <input 
            type="password" 
            id="motDePasse" 
            name="motDePasse" 
            required
            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
            placeholder="Créez un mot de passe sécurisé"
          >
          <p class="mt-2 text-xs text-gray-500">Le mot de passe sera chiffré avant stockage</p>
        </div>

        <!-- Boutons d'action -->
        <div class="flex flex-col sm:flex-row sm:items-center justify-between pt-6 border-t border-gray-200">
          <div class="mb-4 sm:mb-0">
            <a href="/front/utilisateurs" 
               class="inline-flex items-center text-gray-600 hover:text-gray-800">
              <i class="fas fa-arrow-left mr-2"></i>Retour à la liste
            </a>
          </div>
          
          <div class="flex space-x-4">
            <!-- Bouton Annuler -->
            <a href="/front/utilisateurs" 
               class="inline-flex items-center px-5 py-2.5 border border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 transition">
              Annuler
            </a>
            
            <!-- Bouton Créer -->
            <button 
              type="submit"
              class="inline-flex items-center px-5 py-2.5 bg-green-600 text-white font-medium rounded-lg hover:bg-green-700 focus:ring-4 focus:ring-green-300 transition"
            >
              <i class="fas fa-plus mr-2"></i>Créer l'utilisateur
            </button>
          </div>
        </div>
      </form>
    </div>

    <!-- Information -->
    <div class="mt-8 p-4 bg-green-50 border border-green-200 rounded-lg">
      <div class="flex items-start">
        <i class="fas fa-lightbulb text-green-500 mt-0.5 mr-3"></i>
        <div class="text-sm text-green-700">
          <p class="font-medium mb-1">Création de compte</p>
          <p>L'utilisateur pourra se connecter immédiatement après création.</p>
        </div>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>