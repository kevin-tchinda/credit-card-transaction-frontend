<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Créer Transaction - Détection de Fraude</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
  <%- include('../partials/header') %>

  <main class="max-w-2xl mx-auto px-4 py-8">
    <!-- En-tête -->
    <div class="mb-8">
      <div class="flex items-center">
        <a href="/front/transactions" class="text-blue-600 hover:text-blue-800 mr-4">
          <i class="fas fa-arrow-left"></i>
        </a>
        <div>
          <h1 class="text-2xl font-bold text-gray-800">Créer une transaction</h1>
          <p class="text-gray-600 mt-1">Ajouter une nouvelle transaction au système</p>
        </div>
      </div>
    </div>

    <!-- Carte du formulaire -->
    <div class="bg-white rounded-xl shadow-lg border border-gray-200 overflow-hidden">
      <!-- En-tête colorée -->
      <div class="bg-gradient-to-r from-green-600 to-green-700 text-white p-6">
        <div class="flex items-center">
          <div class="w-12 h-12 bg-white/20 rounded-lg flex items-center justify-center mr-4">
            <i class="fas fa-exchange-alt text-xl"></i>
          </div>
          <div>
            <h2 class="text-xl font-bold">Nouvelle transaction</h2>
            <p class="text-green-100">Remplissez les détails de la transaction</p>
          </div>
        </div>
      </div>

      <!-- Formulaire -->
      <form action="/front/transactions/create" method="POST" class="p-8">
        <!-- Message d'erreur -->
        <% if (error) { %>
          <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
            <div class="flex items-center">
              <i class="fas fa-exclamation-circle text-red-500 mr-3"></i>
              <p class="text-red-700"><%= error %></p>
            </div>
          </div>
        <% } %>

        <!-- Montant -->
        <div class="mb-6">
          <label for="montant" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-dollar-sign mr-2 text-gray-500"></i>Montant (CAD)
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <span class="text-gray-500">$</span>
            </div>
            <input 
              type="number" 
              id="montant" 
              name="montant" 
              step="0.01" 
              min="0.01"
              required
              class="pl-8 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
              placeholder="0.00"
            >
            <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
              <span class="text-gray-500">CAD</span>
            </div>
          </div>
          <p class="mt-2 text-xs text-gray-500">
            <i class="fas fa-info-circle mr-1"></i>
            Montant > 1000$ CAD = +40 points de fraude, > 5000$ CAD = +70 points
          </p>
        </div>

        <!-- Pays -->
        <div class="mb-6">
          <label for="paysTransaction" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-globe mr-2 text-gray-500"></i>Lieux (Pays)
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-map-marker-alt text-gray-400"></i>
            </div>
            <input 
              type="text" 
              id="paysTransaction" 
              name="paysTransaction" 
              required
              class="pl-10 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
              placeholder="Ex: Canada, France, USA..."
            >
          </div>
          <p class="mt-2 text-xs text-gray-500">
            <i class="fas fa-info-circle mr-1"></i>
            Pays différent de l'utilisateur = +30 points de fraude
          </p>
        </div>

        <!-- Statut -->
        <div class="mb-6">
          <label for="statut" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-flag mr-2 text-gray-500"></i>Statut
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-tag text-gray-400"></i>
            </div>
            <select 
              id="statut" 
              name="statut" 
              required
              class="pl-10 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition appearance-none"
            >
              <option value="">Sélectionnez un statut</option>
              <option value="ACCEPTEE">ACCEPTEE</option>
              <option value="REFUSEE">REFUSEE</option>
              <option value="EN_ATTENTE">EN_ATTENTE</option>
            </select>
            <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
              <i class="fas fa-chevron-down text-gray-400"></i>
            </div>
          </div>
          <div class="mt-3 grid grid-cols-3 gap-2">
            <div class="text-center p-2 bg-green-50 rounded-lg">
              <div class="text-green-600 text-sm font-medium">ACCEPTEE</div>
              <div class="text-xs text-green-500">Transaction validée</div>
            </div>
            <div class="text-center p-2 bg-red-50 rounded-lg">
              <div class="text-red-600 text-sm font-medium">REFUSEE</div>
              <div class="text-xs text-red-500">Transaction rejetée</div>
            </div>
            <div class="text-center p-2 bg-yellow-50 rounded-lg">
              <div class="text-yellow-600 text-sm font-medium">EN_ATTENTE</div>
              <div class="text-xs text-yellow-500">En attente</div>
            </div>
          </div>
        </div>

        <!-- ID Utilisateur -->
        <div class="mb-8">
          <label for="idUtilisateur" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-user mr-2 text-gray-500"></i>ID Utilisateur
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-id-card text-gray-400"></i>
            </div>
            <input 
              type="number" 
              id="idUtilisateur" 
              name="idUtilisateur" 
              min="1"
              required
              class="pl-10 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500 transition"
              placeholder="Ex: 1, 2, 3..."
            >
          </div>
          <div class="mt-3 flex items-center text-sm text-blue-600">
            <i class="fas fa-lightbulb mr-2"></i>
            <a href="/front/utilisateurs" class="hover:underline">Voir la liste des utilisateurs</a>
          </div>
        </div>

        <!-- Boutons d'action -->
        <div class="flex flex-col sm:flex-row sm:items-center justify-between pt-6 border-t border-gray-200">
          <div class="mb-4 sm:mb-0">
            <a href="/front/transactions" 
               class="inline-flex items-center text-gray-600 hover:text-gray-800">
              <i class="fas fa-arrow-left mr-2"></i>Retour à la liste
            </a>
          </div>
          
          <div class="flex space-x-4">
            <!-- Bouton Annuler -->
            <a href="/front/transactions" 
               class="inline-flex items-center px-5 py-2.5 border border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 transition">
              Annuler
            </a>
            
            <!-- Bouton Créer -->
            <button 
              type="submit"
              class="inline-flex items-center px-5 py-2.5 bg-green-600 text-white font-medium rounded-lg hover:bg-green-700 focus:ring-4 focus:ring-green-300 transition"
            >
              <i class="fas fa-plus mr-2"></i>Créer la transaction
            </button>
          </div>
        </div>
      </form>
    </div>

    <!-- Information sur l'analyse -->
    <div class="mt-8 p-6 bg-blue-50 border border-blue-200 rounded-xl">
      <h3 class="text-lg font-semibold text-blue-800 mb-3">
        <i class="fas fa-brain mr-2"></i>Comment sera analysée cette transaction ?
      </h3>
      <div class="space-y-4">
        <div class="flex items-start">
          <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center mr-3 mt-1">
            <i class="fas fa-calculator text-blue-600"></i>
          </div>
          <div>
            <h4 class="font-medium text-gray-800">Score de fraude automatique</h4>
            <p class="text-gray-600 text-sm">Le système calculera un score basé sur : montant, pays, heure</p>
          </div>
        </div>
        
        <div class="flex items-start">
          <div class="w-8 h-8 bg-red-100 rounded-full flex items-center justify-center mr-3 mt-1">
            <i class="fas fa-exclamation-triangle text-red-600"></i>
          </div>
          <div>
            <h4 class="font-medium text-gray-800">Alerte si fraude détectée</h4>
            <p class="text-gray-600 text-sm">Si score ≥ 70%, une alerte sera générée automatiquement</p>
          </div>
        </div>
        
        <div class="text-sm text-gray-500 italic border-t pt-4">
          <p><i class="fas fa-clock mr-2"></i>L'heure de la transaction sera automatiquement enregistrée à la création</p>
        </div>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>