<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modifier Transaction - Détection de Fraude</title>
  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
  <!-- Header -->
  <%- include('../partials/header') %>

  <main class="max-w-2xl mx-auto px-4 py-8">
    <!-- En-tête -->
    <div class="mb-8">
      <div class="flex items-center">
        <a href="/front/transactions" class="text-blue-600 hover:text-blue-800 mr-4">
          <i class="fas fa-arrow-left"></i>
        </a>
        <div>
          <h1 class="text-2xl font-bold text-gray-800">Modifier une transaction</h1>
          <p class="text-gray-600 mt-1">ID : #<%= transaction.idTransaction %></p>
        </div>
      </div>
      
      <!-- Info transaction -->
      <div class="mt-4 p-4 bg-blue-50 rounded-lg border border-blue-200">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-700">
              <i class="fas fa-history mr-2 text-blue-500"></i>
              Créée le : 
              <span class="font-medium">
                <% 
                  const createdDate = new Date(transaction.dateTransaction || new Date());
                  const formattedCreated = createdDate.toLocaleDateString('fr-FR', {
                    day: '2-digit',
                    month: '2-digit',
                    year: 'numeric',
                    hour: '2-digit',
                    minute: '2-digit'
                  });
                %>
                <%= formattedCreated %>
              </span>
            </p>
          </div>
          <div class="text-sm">
            <% if (transaction.Utilisateur) { %>
              <span class="text-gray-600">Utilisateur : </span>
              <span class="font-medium"><%= transaction.Utilisateur.nom %> <%= transaction.Utilisateur.prenom %></span>
            <% } %>
          </div>
        </div>
      </div>
    </div>

    <!-- Carte du formulaire -->
    <div class="bg-white rounded-xl shadow-lg border border-gray-200 overflow-hidden">
      <!-- En-tête colorée -->
      <div class="bg-gradient-to-r from-blue-600 to-blue-700 text-white p-6">
        <div class="flex items-center">
          <div class="w-12 h-12 bg-white/20 rounded-lg flex items-center justify-center mr-4">
            <i class="fas fa-edit text-xl"></i>
          </div>
          <div>
            <h2 class="text-xl font-bold">Modification</h2>
            <p class="text-blue-100">Mettez à jour les informations de la transaction</p>
          </div>
        </div>
      </div>

      <!-- Formulaire -->
      <form action="/front/transactions/<%= transaction.idTransaction %>/edit" method="POST" class="p-8">
        <!-- Message d'erreur -->
        <% if (error) { %>
          <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
            <div class="flex items-center">
              <i class="fas fa-exclamation-circle text-red-500 mr-3"></i>
              <p class="text-red-700"><%= error %></p>
            </div>
          </div>
        <% } %>

        <!-- Montant -->
        <div class="mb-6">
          <label for="montant" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-dollar-sign mr-2 text-gray-500"></i>Montant (CAD)
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <span class="text-gray-500">$</span>
            </div>
            <input 
              type="number" 
              id="montant" 
              name="montant" 
              step="0.01" 
              min="0.01"
              value="<%= transaction.montant %>" 
              required
              class="pl-8 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
            >
            <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
              <span class="text-gray-500">CAD</span>
            </div>
          </div>
          <div class="mt-2 flex items-center text-sm">
            <% if (transaction.montant > 1000) { %>
              <span class="inline-flex items-center px-2 py-1 bg-red-100 text-red-700 rounded text-xs">
                <i class="fas fa-exclamation-triangle mr-1"></i>Montant élevé (>1000$ CAD)
              </span>
            <% } %>
          </div>
        </div>

        <!-- Date -->
        <div class="mb-6">
          <label for="dateTransaction" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-calendar-alt mr-2 text-gray-500"></i>Date et heure
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-clock text-gray-400"></i>
            </div>
            <input 
              type="datetime-local" 
              id="dateTransaction" 
              name="dateTransaction" 
              value="<%= transaction.dateTransaction ? transaction.dateTransaction.replace('Z','').slice(0,16) : '' %>" 
              required
              class="pl-10 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
            >
          </div>
          <p class="mt-2 text-xs text-gray-500">
            <i class="fas fa-info-circle mr-1"></i>
            Heure entre 0h et 5h = +20 points de fraude
          </p>
        </div>

        <!-- Pays -->
        <div class="mb-6">
          <label for="paysTransaction" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-globe mr-2 text-gray-500"></i>Pays
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-map-marker-alt text-gray-400"></i>
            </div>
            <input 
              type="text" 
              id="paysTransaction" 
              name="paysTransaction" 
              value="<%= transaction.paysTransaction %>" 
              required
              class="pl-10 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition"
              placeholder="Ex: Canada, France, USA..."
            >
          </div>
          <p class="mt-2 text-xs text-gray-500">
            <i class="fas fa-info-circle mr-1"></i>
            Pays différent de l'utilisateur = +30 points de fraude
          </p>
        </div>

        <!-- Statut CORRIGÉ (ACCEPTEE/REFUSEE/EN_ATTENTE) -->
        <div class="mb-8">
          <label for="statut" class="block text-sm font-medium text-gray-700 mb-2">
            <i class="fas fa-flag mr-2 text-gray-500"></i>Statut
          </label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-tag text-gray-400"></i>
            </div>
            <select 
              id="statut" 
              name="statut" 
              required
              class="pl-10 w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition appearance-none"
            >
              <option value="ACCEPTEE" <%= transaction.statut === "ACCEPTEE" ? "selected" : "" %>>ACCEPTEE</option>
              <option value="REFUSEE" <%= transaction.statut === "REFUSEE" ? "selected" : "" %>>REFUSEE</option>
              <option value="EN_ATTENTE" <%= transaction.statut === "EN_ATTENTE" ? "selected" : "" %>>EN_ATTENTE</option>
            </select>
            <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
              <i class="fas fa-chevron-down text-gray-400"></i>
            </div>
          </div>
          
          <!-- Légende des statuts -->
          <div class="mt-3 grid grid-cols-3 gap-2">
            <div class="text-center p-2 <%= transaction.statut === "ACCEPTEE" ? 'bg-green-100 border-2 border-green-300' : 'bg-green-50' %> rounded-lg">
              <div class="<%= transaction.statut === "ACCEPTEE" ? 'text-green-700 font-bold' : 'text-green-600' %> text-sm">ACCEPTEE</div>
              <div class="text-xs <%= transaction.statut === "ACCEPTEE" ? 'text-green-600' : 'text-green-500' %>">Transaction acceptée</div>
            </div>
            <div class="text-center p-2 <%= transaction.statut === "REFUSEE" ? 'bg-red-100 border-2 border-red-300' : 'bg-red-50' %> rounded-lg">
              <div class="<%= transaction.statut === "REFUSEE" ? 'text-red-700 font-bold' : 'text-red-600' %> text-sm">REFUSEE</div>
              <div class="text-xs <%= transaction.statut === "REFUSEE" ? 'text-red-600' : 'text-red-500' %>">Transaction refusée</div>
            </div>
            <div class="text-center p-2 <%= transaction.statut === "EN_ATTENTE" ? 'bg-yellow-100 border-2 border-yellow-300' : 'bg-yellow-50' %> rounded-lg">
              <div class="<%= transaction.statut === "EN_ATTENTE" ? 'text-yellow-700 font-bold' : 'text-yellow-600' %> text-sm">EN_ATTENTE</div>
              <div class="text-xs <%= transaction.statut === "EN_ATTENTE" ? 'text-yellow-600' : 'text-yellow-500' %>">En attente</div>
            </div>
          </div>
        </div>

        <!-- Boutons d'action -->
        <div class="flex flex-col sm:flex-row sm:items-center justify-between pt-6 border-t border-gray-200">
          <div class="mb-4 sm:mb-0">
            <a href="/front/transactions" 
               class="inline-flex items-center text-gray-600 hover:text-gray-800">
              <i class="fas fa-arrow-left mr-2"></i>Retour à la liste
            </a>
          </div>
          
          <div class="flex space-x-4">
            <!-- Bouton Annuler -->
            <a href="/front/transactions" 
               class="inline-flex items-center px-5 py-2.5 border border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 transition">
              Annuler
            </a>
            
            <!-- Bouton Mettre à jour -->
            <button 
              type="submit"
              class="inline-flex items-center px-5 py-2.5 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 focus:ring-4 focus:ring-blue-300 transition"
            >
              <i class="fas fa-save mr-2"></i>Mettre à jour
            </button>
          </div>
        </div>
      </form>
    </div>

    <!-- Information analyse -->
    <div class="mt-8 p-6 bg-purple-50 border border-purple-200 rounded-xl">
      <h3 class="text-lg font-semibold text-purple-800 mb-3">
        <i class="fas fa-chart-line mr-2"></i>Impact sur l'analyse de fraude
      </h3>
      <div class="space-y-3">
        <div class="flex items-center text-sm">
          <div class="w-6 h-6 bg-purple-100 rounded-full flex items-center justify-center mr-3">
            <i class="fas fa-sync-alt text-purple-600 text-xs"></i>
          </div>
          <p class="text-gray-700">La modification déclenchera une <span class="font-medium">ré-analyse automatique</span> de la transaction.</p>
        </div>
        
        <div class="flex items-center text-sm">
          <div class="w-6 h-6 bg-purple-100 rounded-full flex items-center justify-center mr-3">
            <i class="fas fa-calculator text-purple-600 text-xs"></i>
          </div>
          <p class="text-gray-700">Le score de fraude sera <span class="font-medium">recalculé</span> avec les nouvelles valeurs.</p>
        </div>
        
        <% if (transaction.Analyse) { %>
        <div class="mt-4 p-3 bg-white rounded-lg border">
          <div class="flex items-center justify-between">
            <div>
              <p class="text-sm font-medium text-gray-800">Analyse existante</p>
              <p class="text-xs text-gray-600">Score : <%= transaction.Analyse.score %>% • 
                <% if (transaction.Analyse.isFraude) { %>
                  <span class="text-red-600">Fraude détectée</span>
                <% } else { %>
                  <span class="text-green-600">Normale</span>
                <% } %>
              </p>
            </div>
            <a href="/front/analyses/<%= transaction.Analyse.idAnalyse %>" class="text-xs text-purple-600 hover:text-purple-800">
              <i class="fas fa-external-link-alt mr-1"></i>Voir
            </a>
          </div>
        </div>
        <% } %>
      </div>
    </div>

    <!-- Danger zone -->
    <div class="mt-6 p-6 bg-red-50 border border-red-200 rounded-xl">
      <h3 class="text-lg font-semibold text-red-800 mb-3">
        <i class="fas fa-exclamation-triangle mr-2"></i>Zone de prudence
      </h3>
      <ul class="space-y-2 text-sm text-red-700">
        <li class="flex items-start">
          <i class="fas fa-circle text-red-500 text-xs mt-1 mr-2"></i>
          <span>Modifier une transaction existante peut affecter les analyses et statistiques.</span>
        </li>
        <li class="flex items-start">
          <i class="fas fa-circle text-red-500 text-xs mt-1 mr-2"></i>
          <span>Les transactions marquées comme "REFUSEE" seront surveillées plus attentivement.</span>
        </li>
        <li class="flex items-start">
          <i class="fas fa-circle text-red-500 text-xs mt-1 mr-2"></i>
          <span>Consultez l'onglet "Analyses" après modification pour voir le nouveau score.</span>
        </li>
      </ul>
    </div>
  </main>

  <!-- Footer -->
  <%- include('../partials/footer') %>
</body>
</html>